spring:
  datasource:
    url: jdbc:postgresql://localhost:5433/student_db
    username: sms_user
    password: sms_pass
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

server:
  port: 8080
