apiVersion: v1
kind: Service
metadata:
  name: frontend
  namespace: sms
  labels:
    app: sms-frontend
spec:
  type: LoadBalancer
  selector:
    app: sms-frontend
  ports:
    - name: http
      port: 80
      targetPort: 80
