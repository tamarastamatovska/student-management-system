apiVersion: v1
kind: Service
metadata:
  name: backend
  namespace: sms
  labels:
    app: sms-backend
spec:
  type: ClusterIP
  selector:
    app: sms-backend
  ports:
    - name: http
      port: 8080
      targetPort: 8080
